// Copyright © 2026 One Identity LLC. ALL RIGHTS RESERVED.
/** Internal state representation used in Figma token mapping. Not a component input; the toggle is controlled via `ngModel` or reactive forms. */
export type ToggleState = 'on' | 'off';
