// Copyright © 2026 One Identity LLC. ALL RIGHTS RESERVED.
/** Visual size of the avatar. */
export type AvatarSize = 'sm' | 'md' | 'default' | 'lg';
/** Entity type rendered inside the avatar; determines the icon or face image used. */
export type AvatarType = 'face' | 'placeholder' | 'machine' | 'service-account' | 'workload' | 'bot' | 'ai-agent';
/** Broad identity category: `'human'` for people, `'nhi'` for non-human identities. */
export type AvatarCategory = 'human' | 'nhi';
