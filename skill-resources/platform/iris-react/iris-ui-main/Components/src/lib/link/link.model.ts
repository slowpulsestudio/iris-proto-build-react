// Copyright © 2026 One Identity LLC. ALL RIGHTS RESERVED.
/** Visual size of the link. */
export type LinkSize = 'default' | 'lg';
/** Internal visual state used in Figma token mapping and story controls. Not a component input. */
export type LinkState = 'default' | 'hover' | 'focus' | 'disabled' | 'visited';
/** Value for the HTML `target` attribute on the underlying anchor element. */
export type LinkTarget = '_blank' | '_self' | '_parent' | '_top';
