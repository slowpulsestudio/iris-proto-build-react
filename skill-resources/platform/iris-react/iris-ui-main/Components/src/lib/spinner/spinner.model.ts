// Copyright © 2026 One Identity LLC. ALL RIGHTS RESERVED.
/** Visual size of the spinner. */
export type SpinnerSize = 'sm' | 'default' | 'lg';
/** Animation scenario. `'loop'` spins indefinitely; `'completion'` plays a one-shot success animation. */
export type SpinnerScenario = 'loop' | 'completion';
