{/* Copyright © 2026 One Identity LLC. ALL RIGHTS RESERVED. */}
import { Meta, Canvas, ArgTypes, Controls } from '@storybook/addon-docs/blocks';
import * as TextInputStories from './textinput.stories';

<Meta of={TextInputStories} />

# TextInput

A single-line text field built on a native `<input>`. It is designed to compose with `iris-form-field`, `iris-label`, and `iris-subtext`, but it can also be used standalone when label and feedback are handled elsewhere.

Current implementation supports visual size, disabled / readonly state, and decorative leading / trailing icons. It does not expose a `type` input, so rendered native input behaves as default text input only.

## Interactive Demo

<Canvas of={TextInputStories.Overview} />
<Controls of={TextInputStories.Overview} />

## Sizes

Two visual sizes are available: `default` and `lg`.

<Canvas of={TextInputStories.Default} />
<Canvas of={TextInputStories.Large} />

## Using without Form Field

Use `iris-textinput` standalone when wrapper-managed label, hint, error, and counter UI are not needed.

<Canvas of={TextInputStories.WithoutFormField} />

## Disabled state

Apply `[disabled]="true"` to prevent focus and editing. A form control disabled through Angular forms produces same result.

<Canvas of={TextInputStories.Disabled} />

## Readonly

A readonly input remains focusable and selectable, but the user cannot edit its value.

<Canvas of={TextInputStories.Readonly} />

## Hint text

Provide contextual guidance below input with `iris-subtext`.

<Canvas of={TextInputStories.WithHint} />

## Validation error

Connect input to reactive form control. Error styling appears only when bound control is both invalid and touched. `iris-form-field` shows first matching `iris-subtext type="error"` and hides hint text.

```html
<iris-form-field>
  <iris-label>Email</iris-label>
  <iris-textinput [formControl]="emailControl" placeholder="you@example.com"></iris-textinput>
  <iris-subtext type="hint">Enter your work email address.</iris-subtext>
  @if (emailControl.hasError('required')) {
    <iris-subtext type="error">Email is required.</iris-subtext>
  }
  @if (emailControl.hasError('email')) {
    <iris-subtext type="error">Enter a valid email address.</iris-subtext>
  }
</iris-form-field>
```

<Canvas of={TextInputStories.WithValidationError} />

## Character count

Add `Validators.maxLength(n)` to bound form control. Current implementation uses validator metadata to drive label counter through `iris-form-field`, but it does not set native `maxlength` on `<input>`.

<Canvas of={TextInputStories.WithCharacterCount} />

## Icons

Use `leadingIcon` and `trailingIcon` to place decorative icons inside field chrome. Current implementation does not make those icons interactive and does not provide a clear button.

<Canvas of={TextInputStories.WithIcons} />

## Accessibility

- `iris-label` provides label association when composed inside `iris-form-field`.
- Native text-input keyboard behaviour is preserved; component adds no custom shortcuts.
- `aria-invalid` is set only when attached form control exists and is both invalid and touched.
- Required state comes from `Validators.required` on bound form control; there is no separate `required` input.
- Readonly inputs stay focusable. Disabled inputs do not.

## API

<ArgTypes of={TextInputStories} sort="alpha" />
